# 认证架构 — 完整分层设计

> **更新日期**：2026-06-03
> **技术栈**：Next.js 16 (App Router) + Supabase Auth + @supabase/ssr v0.10+
> **关联文档**：[04认证组件.md](./04认证组件.md)（UI 组件拆分方案）、[12数据表设计.md](./12数据表设计.md)（数据库模型）
> **架构原则**：严格七层单向依赖，上层调用下层，下层绝不依赖上层

---

## 一、架构总览

```
                        ┌─────────────────────────────┐
                        │   proxy.ts（根目录）           │  请求入口 — 每次请求都经过
                        │   export proxy()              │  Session 刷新 + 路由守卫
                        └─────────────┬───────────────┘
                                      │ 调用
                        ┌─────────────▼───────────────┐
                        │ lib/supabase/proxy.ts        │  Supabase Cookie 管理
                        │   updateSession()             │  createServerClient + cookies
                        └──────────────────────────────┘
        ═══════════════════════════════════════════════════════════════
        以下为客户端分层（浏览器端执行）

  ┌──────────────────────────────────────────────────────────────┐
  │  ＵＩ 层  (components/auth/ + app/(auth)/)                   │
  │  只负责渲染和事件派发，不包含业务逻辑                           │
  │  AuthPage → AuthCard → TabSwitcher / AuthForm / SocialLogin  │
  └────────────────────┬─────────────────────────────────────────┘
                       │ useAuth()
  ┌────────────────────▼─────────────────────────────────────────┐
  │  状 态 层  (lib/auth-context.tsx)                            │
  │  React Context — AuthProvider + useAuth() hook               │
  │  管理 user / isLoading / authError 全局状态                   │
  │  调用 repositories/supabase-auth.ts                          │
  └────────────────────┬─────────────────────────────────────────┘
                       │ 调用
  ┌────────────────────▼─────────────────────────────────────────┐
  │  数 据 访 问 层  (repositories/)                             │
  │  ISupabaseAuthRepository 接口 + SupabaseAuthRepository       │
  │  signIn / signUp / signOut / getSession / updateProfile      │
  └────────────────────┬─────────────────────────────────────────┘
                       │ 调用
  ┌────────────────────▼─────────────────────────────────────────┐
  │  数 据 层  (Supabase Auth API)                               │
  │  @supabase/supabase-js → supabase.auth.*                     │
  │  Supabase 服务端处理 bcrypt 哈希、JWT 签发/验证               │
  └──────────────────────────────────────────────────────────────┘
        ═══════════════════════════════════════════════════════════════
        基础设施层（被所有层引用）

  ┌──────────────────────────────────────────────────────────────┐
  │  基 础 设 施 层                                              │
  │  lib/supabase/client.ts — 浏览器端 Supabase Client           │
  │  lib/supabase/server.ts — 服务端 Supabase Client             │
  │  types/auth.ts — User, LoginPayload, RegisterPayload ...     │
  │  utils/validators.ts — validateFullName, validateEmail ...   │
  │  .env.local — NEXT_PUBLIC_SUPABASE_URL / PUBLISHABLE_KEY     │
  └──────────────────────────────────────────────────────────────┘
```

### 核心依赖规则

| 规则 | 说明 |
|------|------|
| **proxy.ts 先行** | 请求先经过 `proxy.ts`，完成 Session 验证后再到达页面 |
| **状态层统一入口** | 所有组件通过 `useAuth()` 获取认证状态，不直接调 repository |
| **密码永不到达前端** | bcrypt 哈希在 Supabase 服务端完成，前端只传明文密码 |
| **Session 存储在 httpOnly cookie** | 不由前端 JS 直接读写，防 XSS 窃取 |
| **类型安全** | `AuthResponse` 为 discriminated union，禁止 `any` |

---

## 二、proxy.ts — 入口守卫（Next.js 16 标准模式）

### 2.1 为什么用 proxy.ts 而非 middleware.ts

| 模式 | 状态 | 说明 |
|------|------|------|
| `middleware.ts` | **已废弃** | Next.js 16 中 `@supabase/ssr` 推荐使用 `proxy.ts` |
| `proxy.ts` | **当前标准** | 更好的命名，与 Next.js 内置 proxy 机制对齐 |

### 2.2 执行流程

```
浏览器请求 /workspace
        │
        ▼
  proxy.ts (export async function proxy())
        │
        ├─► createServerClient (读取 request cookies)
        │
        ├─► supabase.auth.getUser()
        │
        ├─► 无 user + 受保护路由？ → 302 redirect → /login
        ├─► 有 user + /login？    → 302 redirect → /workspace
        │
        └─► 正常放行 + cookie 刷新 → NextResponse.next()
```

### 2.3 文件职责

| 文件 | 职责 | 运行环境 |
|------|------|---------|
| `proxy.ts`（根目录） | 导出 `proxy()` 入口函数 + `config.matcher` | Edge Runtime |
| `lib/supabase/proxy.ts` | `updateSession()` — 创建 Server Client、调用 `getUser()`、路由守卫逻辑 | Edge Runtime |

### 2.4 proxy.ts 代码

```typescript
// proxy.ts — 项目根目录
import { updateSession } from '@/lib/supabase/proxy'
import type { NextRequest } from 'next/server'

export async function proxy(request: NextRequest) {
  return await updateSession(request)
}

export const config = {
  matcher: [
    // 排除静态资源，其余所有请求都走 proxy
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
}
```

```typescript
// lib/supabase/proxy.ts
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function updateSession(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,
    {
      cookies: {
        getAll() { return request.cookies.getAll() },
        setAll(cookiesToSet) {
          for (const { name, value } of cookiesToSet) {
            request.cookies.set(name, value)
          }
          supabaseResponse = NextResponse.next({ request })
          for (const { name, value, options } of cookiesToSet) {
            supabaseResponse.cookies.set(name, value, options)
          }
        },
      },
    }
  )

  const { data: { user } } = await supabase.auth.getUser()

  const protectedPaths = ['/workspace', '/trash']
  const isProtectedRoute = protectedPaths.some((p) => request.nextUrl.pathname.startsWith(p))
  const isAuthRoute = request.nextUrl.pathname === '/login'

  if (!user && isProtectedRoute) {
    return NextResponse.redirect(new URL('/login', request.url))
  }
  if (user && isAuthRoute) {
    return NextResponse.redirect(new URL('/workspace', request.url))
  }

  return supabaseResponse
}
```

### 2.5 路由守卫矩阵

| 用户状态 | 访问 `/login` | 访问 `/workspace`, `/trash` | 访问 `/`（官网） |
|----------|-------------|---------------------------|---------------|
| 未登录 | 正常显示 | **302 → /login** | 正常显示 |
| 已登录 | **302 → /workspace** | 正常显示 | 正常显示 |

---

## 三、基础设施层

### 3.1 Supabase Client（三种场景）

| 文件 | 场景 | API |
|------|------|-----|
| `lib/supabase/client.ts` | 浏览器端（组件/hooks） | `createBrowserClient()` |
| `lib/supabase/server.ts` | 服务端（Server Component/Route Handler） | `createServerClient() + cookies()` |
| `lib/supabase/proxy.ts` | Edge Runtime（proxy 拦截） | `createServerClient() + request.cookies` |

### 3.2 环境变量

```env
# .env.local
NEXT_PUBLIC_SUPABASE_URL=https://axqovqvxtuhmxeftkddm.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_k1Vt3cZnlz0QTUQKHyLlcg_-vzW2oaG
```

`PUBLISHABLE_KEY` 是 anon key，前端安全使用。不暴露 `service_role` key。

---

## 四、类型定义层

### 4.1 types/auth.ts

```typescript
// 与 Supabase Auth User 对齐，附加业务字段
export interface User {
  id: string          // UUID（Supabase Auth 统一使用 uuid）
  uid: string         // 公开短 ID
  email: string
  phone?: string
  fullName: string    // 映射 DB: full_name
  avatar: string      // 首字字符
  membership: 'free' | 'pro' | 'enterprise'
  createdAt: string
  updatedAt: string
}

export interface LoginPayload {
  email: string
  password: string
  rememberMe?: boolean
}

export interface RegisterPayload {
  fullName: string
  email: string
  password: string
}

// ✦ Discriminated union — success 时必有 user，error 时必有 error
export type AuthResponse =
  | { success: true; user: User }
  | { success: false; error: string }
```

### 4.2 前端类型 ↔ Supabase 字段映射

| types/auth.ts | Supabase Auth User | 说明 |
|--------------|-------------------|------|
| `id` | `user.id` | UUID 直接映射 |
| `uid` | `user_metadata.uid` | 自定义，注册时写入 |
| `email` | `user.email` | Supabase 管理 |
| `fullName` | `user_metadata.full_name` | 自定义，snake_case → camelCase |
| `avatar` | `user_metadata.avatar` | 自定义，首字字符 |
| `phone` | `user_metadata.phone` | 自定义 |
| `membership` | `user_metadata.membership` | 自定义，默认 'free' |
| `createdAt` | `user.created_at` | Supabase 自动 |
| `updatedAt` | `user.updated_at` | Supabase 自动 |

**永远不返回给前端**：`password`, `password_hash`, `encrypted_password`

---

## 五、数据访问层（Repository）

### 5.1 ISupabaseAuthRepository 接口

```typescript
// repositories/types.ts
export interface ISupabaseAuthRepository {
  signIn(email: string, password: string): Promise<AuthResponse>
  signUp(payload: RegisterPayload): Promise<AuthResponse>
  signOut(): Promise<void>
  getSession(): Promise<User | null>
  onAuthStateChange(callback: (user: User | null) => void): () => void
  updatePassword(newPassword: string): Promise<{ success: boolean; error?: string }>
  updateProfile(updates: Partial<User>): Promise<{ success: boolean; error?: string }>
  getRecentSearches(): string[]
  addRecentSearch(term: string): string[]
  removeRecentSearch(term: string): string[]
}
```

### 5.2 mapUser — Supabase User → 前端 User

```typescript
// repositories/supabase-auth.ts
private mapUser(supabaseUser: SupabaseUser): User {
  const metadata = supabaseUser.user_metadata ?? {}
  return {
    id: supabaseUser.id,
    uid: metadata.uid ?? supabaseUser.id.slice(0, 8),
    email: supabaseUser.email ?? '',
    phone: metadata.phone ?? undefined,
    fullName: metadata.full_name ?? '',
    avatar: (metadata.full_name as string)?.charAt(0) ?? '用',
    membership: metadata.membership ?? 'free',
    createdAt: supabaseUser.created_at,
    updatedAt: supabaseUser.updated_at ?? supabaseUser.created_at,
  }
}
```

### 5.3 导出实例

```typescript
// repositories/index.ts
export const supabaseAuthRepo: ISupabaseAuthRepository = new SupabaseAuthRepository()
```

---

## 六、状态层（React Context）

### 6.1 AuthProvider 数据流

```
AuthProvider (root layout)
  │
  ├─ mount 时:
  │    supabaseAuthRepo.getSession() → setUser(sessionUser)
  │    supabaseAuthRepo.onAuthStateChange() → 监听多标签页同步
  │
  ├─ login(email, password):
  │    1. supabaseAuthRepo.signIn(email, password)
  │    2. 成功 → setUser(result.user)
  │    3. 失败 → setAuthError(result.error)
  │    4. 返回 { success, error? }
  │
  ├─ register(payload):
  │    1. supabaseAuthRepo.signUp(payload)
  │    2. 成功 → setUser(result.user)
  │    3. 失败 → setAuthError(result.error)
  │
  ├─ logout():
  │    1. supabaseAuthRepo.signOut()
  │    2. setUser(null)
  │
  ├─ updateUser(updates):
  │    1. setUser(prev → { ...prev, ...updates })  // 先更新本地
  │    2. supabaseAuthRepo.updateProfile(updates)   // 再同步 Supabase
  │
  └─ changePassword(newPassword):
       1. supabaseAuthRepo.updatePassword(newPassword)
       2. 返回 boolean
```

### 6.2 useAuth() 返回值

```typescript
interface AuthContextValue {
  user: User | null
  isLoading: boolean
  isAuthenticated: boolean    // !!user 的快捷访问
  login: (email: string, password: string) => Promise<{ success: boolean; error?: string }>
  register: (payload: RegisterPayload) => Promise<{ success: boolean; error?: string }>
  logout: () => Promise<void>
  logoutAndClear: () => Promise<void>
  updateUser: (updates: Partial<User>) => Promise<void>
  changePassword: (newPassword: string) => Promise<boolean>
  clearError: () => void
  authError: string | null
}
```

---

## 七、UI 层 — 客户端路由守卫

### 7.1 双层守卫机制

```
  请求到达
    │
    ▼
  ┌─────────────────────────────────────┐
  │  第一层：proxy.ts（服务端）           │  ← Edge Runtime，每次请求都执行
  │  - Cookie 验证                       │
  │  - 路由重定向（302）                  │
  │  - Session 刷新                      │
  └─────────────────────────────────────┘
    │ 放行
    ▼
  ┌─────────────────────────────────────┐
  │  第二层：AuthGuard / AuthLayout（客户端）│  ← 浏览器端，处理加载态
  │  - 未登录时显示 spinner              │
  │  - useEffect 中 router.replace       │
  └─────────────────────────────────────┘
```

### 7.2 AuthGuard（受保护路由）

```typescript
// components/layout/auth-guard.tsx
// 用于 (main)/layout.tsx → 包裹 workspace + trash
// isLoading=true → spinner
// !isAuthenticated → spinner + redirect /login
// isAuthenticated → 渲染 children
```

### 7.3 AuthLayout（认证路由）

```typescript
// app/(auth)/layout.tsx
// 用于 login page
// isLoading=true || isAuthenticated=true → 统一 spinner
// 其余 → 渲染 children
// useEffect → router.replace('/workspace')
```

### 7.4 组件树

```
app/layout.tsx
  └── AuthProvider (React Context)
        │
        ├── app/(auth)/layout.tsx (AuthLayout)
        │     └── login/page.tsx → <AuthPage>
        │           ├── <AuthCard>
        │           ├── <TabSwitcher>              登录/注册切换
        │           ├── <AuthForm>                 统一表单
        │           │     ├── <FormInput> × N
        │           │     ├── <Checkbox>
        │           │     └── <SubmitButton>
        │           └── <SocialLogin>              Google / Apple
        │
        └── app/(main)/layout.tsx (AuthGuard)
              ├── /workspace/page.tsx
              └── /trash/page.tsx
```

---

## 八、完整文件清单

### 8.1 认证相关文件

| 层级 | 文件 | 说明 |
|------|------|------|
| **入口守卫** | `proxy.ts` | 根目录，`export proxy()` 入口 |
| **入口守卫** | `lib/supabase/proxy.ts` | `updateSession()` — Cookie 管理 + 路由守卫 |
| **基础设施** | `lib/supabase/client.ts` | 浏览器端 `createBrowserClient` |
| **基础设施** | `lib/supabase/server.ts` | 服务端 `createServerClient`（Server Component） |
| **基础设施** | `utils/validators.ts` | `validateFullName`, `validateEmail`, `validatePasswordStrength`, `validateConfirmPassword` |
| **类型** | `types/auth.ts` | `User`, `LoginPayload`, `RegisterPayload`, `AuthResponse` |
| **数据访问** | `repositories/supabase-auth.ts` | `SupabaseAuthRepository` 实现所有认证操作 |
| **数据访问** | `repositories/types.ts` | `ISupabaseAuthRepository` 接口 |
| **数据访问** | `repositories/index.ts` | `supabaseAuthRepo` 导出 |
| **状态** | `lib/auth-context.tsx` | `AuthProvider` + `useAuth()` |
| **UI-布局** | `components/layout/auth-guard.tsx` | 受保护路由守卫 |
| **UI-布局** | `app/(auth)/layout.tsx` | 认证路由守卫（已登录→跳转） |
| **UI-布局** | `app/(main)/layout.tsx` | 受保护路由布局（包裹 AuthGuard） |
| **UI-页面** | `app/(auth)/login/page.tsx` | 登录页 → `<AuthPage>` |
| **UI-组件** | `components/auth/auth-page.tsx` | 认证页编排器 |
| **UI-组件** | `components/auth/auth-card.tsx` | 白色卡片容器 |
| **UI-组件** | `components/auth/tab-switcher.tsx` | 登录/注册 Tab |
| **UI-组件** | `components/auth/AuthForm.tsx` | 统一表单（登录/注册） |
| **UI-组件** | `components/auth/SocialLogin.tsx` | Google/Apple 社交登录按钮 |
| **Hook** | `hooks/use-auth-tab.ts` | Tab 切换动画状态 |
| **环境** | `.env.local` | `NEXT_PUBLIC_SUPABASE_URL` + `PUBLISHABLE_KEY` |

### 8.2 已废弃文件（可归档）

| 文件 | 原因 | 替代 |
|------|------|------|
| ~~`lib/services/auth-service.ts`~~ | 已删除 | `repositories/supabase-auth.ts` |
| ~~`lib/services/userService.ts`~~ | 已删除 | `supabaseAuthRepo.updatePassword()` / `updateProfile()` |
| ~~`lib/supabase/middleware.ts`~~ | 已删除 | `lib/supabase/proxy.ts` |

### 8.3 保留的 Mock 代码（兼容过渡期）

| 文件 | 说明 |
|------|------|
| `data/mockUsers.ts` | 仍被 `userRepo` 导出，非 auth 模块可能引用 |
| `data/session-mock.ts` | 仍被 `sessionRepo` 导出 |
| `repositories/mock-users.ts` | Mock 实现 |
| `repositories/mock-session.ts` | Mock 实现 |

> `repositories/mock-workspace.ts`、`data/workspace-mock.ts`、`store/workspaceStore.ts` 已删除。工作区用户数据统一通过 `useAuth().user` 获取。

---

## 九、认证流程时序图

### 9.1 登录流程

```
用户填写邮箱/密码 → 点击"登录"
        │
        ▼
AuthForm.handleSubmit()
        │
        ▼
auth-page.tsx → handleLoginSubmit()
        │
        ▼
useAuth().login(email, password)
        │
        ▼
supabaseAuthRepo.signIn(email, password)
        │
        ▼
supabase.auth.signInWithPassword()   ← Supabase JS SDK
        │
        ▼
Supabase 服务端:
  - 验证邮箱 + bcrypt 比对 password_hash
  - 签发 JWT access_token + refresh_token
  - 设置 httpOnly cookie（通过 proxy.ts 写入）
        │
        ▼
返回 { data: { user, session } }
        │
        ▼
mapUser(data.user) → 前端 User 类型
        │
        ▼
setUser(user) → 触发 React re-render
        │
        ▼
router.push('/workspace')
```

### 9.2 注册流程

```
用户填写信息 → 点击"注册"
        │
        ▼
supabaseAuthRepo.signUp(payload)
        │
        ▼
supabase.auth.signUp({
  email, password,
  options: { data: { full_name: payload.fullName } }
})
        │
        ▼
Supabase:
  - 检查 email 唯一
  - bcrypt 生成 password_hash
  - 写入 auth.users 表
        │
        ▼
返回 user → 自动登录 → router.push('/workspace')
```

### 9.3 登出流程

```
用户点击"退出登录"
        │
        ▼
useAuth().logout()
        │
        ▼
supabaseAuthRepo.signOut()
        │
        ▼
supabase.auth.signOut()
  - Supabase 清除服务端 session
  - Cookie 被标记过期
        │
        ▼
setUser(null) → React re-render
router.push('/login')
```

### 9.4 修改 Profile 流程（含侧边栏同步）

```
用户修改姓名/手机/头像
        │
        ▼
AccountTab → updateUser({ fullName: '新名' })
        │
        ├─► 1. setUser(prev → { ...prev, ...updates })  ← 即时 UI 更新
        │       → AuthProvider 重新渲染
        │       → UserSection（workspace 侧边栏）通过 useAuth().user 自动同步
        │
        └─► 2. supabaseAuthRepo.updateProfile(updates)  ← 异步持久化
                  │
                  ▼
              supabase.auth.updateUser({ data: { full_name: '新名' } })
                  │
                  ▼
              写入 auth.users.raw_user_meta_data 字段
                  │
                  ▼
              下次 getSession() / onAuthStateChange 自动带回
```

---

## 十、安全要点

| 安全项 | 措施 |
|--------|------|
| **密码存储** | Supabase 服务端 bcrypt 哈希，前端永不接触 hash |
| **Session 存储** | httpOnly cookie（通过 `@supabase/ssr` 管理），不可被 JS 读取 |
| **CSRF 防护** | Supabase Auth 内置 CSRF token 机制 |
| **XSS 防护** | User 对象不包含敏感字段（无 password_hash），localStorage 仅存搜索记录 |
| **速率限制** | Supabase Auth 服务端内置登录频率限制 |
| **Token 过期** | JWT 短期 access_token (1h) + 长期 refresh_token，proxy.ts 自动刷新 |
| **多设备** | `onAuthStateChange` 监听跨标签页同步，登出一处即全部登出 |

---

## 十一、开发指南

### 11.1 添加新的认证功能（步骤）

1. **类型定义** — 在 `types/auth.ts` 添加/修改类型
2. **Repository 接口** — 在 `repositories/types.ts` 的 `ISupabaseAuthRepository` 添加方法签名
3. **Repository 实现** — 在 `repositories/supabase-auth.ts` 实现，调用 `supabase.auth.*`
4. **状态层暴露** — 在 `lib/auth-context.tsx` 的 `AuthContextValue` 和 `value` 中暴露
5. **UI 调用** — 组件中 `const { newMethod } = useAuth()` 使用

### 11.2 调试提示

- `proxy.ts` 日志在 Edge Runtime 中，通过浏览器 DevTools Network 面板查看 302 重定向
- Supabase Dashboard → Authentication → Users 查看注册用户
- Supabase Dashboard → Authentication → Settings → Email Templates 配置邮件模板
- 开发阶段在 Supabase Dashboard 关闭 "Confirm email" 可跳过邮箱验证

### 11.3 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 登录后无限重定向 | proxy.ts 中 cookie 未正确设置 | 检查 `setAll` 的 `request.cookies.set` + `supabaseResponse` 双重写入 |
| `AuthProvider` 报错 "not wrapped" | `useAuth()` 在 `<AuthProvider>` 外部调用 | 确认 `app/layout.tsx` 中 `AuthProvider` 包裹所有 `children` |
| `updateUser` 未持久化 | Supabase `user_metadata` 写入限制 | `user_metadata` 仅支持一层深度，复杂数据入 `public.users` 表 |
| 生产环境需要邮箱确认 | 开发环境关闭确认可跳过 | 正式上线前重新开启，配置邮件模板 |
