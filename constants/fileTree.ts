export const MAX_DEPTH = 5

export const ILLEGAL_CHARS_REGEX = /[\\/:*?"<>|]/

export const DEFAULT_NAME = "未命名"

export const MAX_NAME_LENGTH = 100

/** 默认根节点 ID，仅在前端使用，不与数据库交互 */
export const DEFAULT_ROOT_ID = "__default_root__"
